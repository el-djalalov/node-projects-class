# Dependencies
node_modules/

# Project data files (students generate these locally — don't commit them)
tasks.json
expenses.json
*.csv

# Environment / secrets
.env
.env.*

# OS / editor noise
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
