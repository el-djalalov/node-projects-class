Broadcast Server (submission).

Run:

```bash
npm install
node broadcast-server.js start   # in terminal 1
node broadcast-server.js connect # in terminal 2
```

Type messages in the connect terminal; they broadcast to all other clients.
